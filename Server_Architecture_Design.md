### MMORPG 서버 아키텍처 및 설계 분석

본 문서는 현재 프로젝트의 고가용성 MMORPG 서버 아키텍처를 이력서 및 기술 블로그용으로 정리한 기술 명세서입니다. 대규모 트래픽 처리와 안정적인 분산 시스템 설계에 초점을 맞추고 있습니다.

---

### 1. 아키텍처 구조 (Architecture Structure)
대규모 동시 접속자를 수용하기 위해 역할을 분리한 **분산 서버 아키텍처**를 채택하고 있습니다.

*   **Lobby Server**: 인증, 캐릭터 생성/선택 및 계정 레벨의 데이터 관리.
*   **Play Server**: 실제 게임 로직(전투, 이동, 퀘스트)이 이루어지는 메인 컨텐츠 서버.
*   **Match Server**: 파티 매칭 및 대전 컨텐츠를 전담하여 컨텐츠별 부하를 독립적으로 처리.
*   **Manage Server**: 서버 전체 상태 모니터링 및 실시간 설정 변경 관리.
*   **Common Library**: IOCP 기반 네트워크 라이브러리(SASIO), 공용 컨테이너, 데이터베이스 에이전트를 공유하여 일관성 있는 성능 유지.

---

### 2. 트래픽 급증 대응 전략 (Traffic Surge Strategy)
특정 지역이나 시간에 집중되는 트래픽을 효율적으로 분산하기 위해 **공간 및 로직 분할 기술**을 사용합니다.

*   **다중 채널 및 존(Zone) 시스템**: 동일 지역을 여러 채널로 분리하여 한 지역에 수천 명의 사용자가 몰리는 현상을 방지하고, 물리적 서버 자원을 유연하게 할당합니다.
*   **섹터(Sector) 기반 부하 분산**: 맵을 섹터 단위로 쪼개어 가시 범위(Interest Management) 내의 데이터만 전송함으로써 네트워크 대역폭 및 CPU 연산 비용을 최적화합니다.
*   **침공 및 필드 보스 컨텐츠 관리**: `InvasionManager`를 통해 동적인 컨텐츠 발생 시 해당 세션의 로직 부하를 관리하고 전용 에이전트(`InvasionAgent`)가 독립적인 생명주기를 가집니다.

---

### 3. 분산 처리 (Distributed Processing)
서버 간 부하 분산 및 효율적인 데이터 동기화를 위해 다음과 같은 기술을 적용했습니다.

*   **서버 역할 분할**: 인증(Lobby), 게임(Play), 매칭(Match) 서버를 물리적으로 분리하여 특정 서비스 장애가 전체 시스템으로 전파되는 것을 차단합니다.
*   **에이전트(Agent) 패턴**: 특정 기능(침공, 기여도, 연금술 등)을 담당하는 Agent 객체를 두어 비즈니스 로직을 모듈화하고 병렬 처리가 용이한 구조로 설계했습니다.
*   **TSafeContainer 기반 동기화**: `TSafeMap`, `TSafeList` 등 스레드 안전한 커스텀 컨테이너를 사용하여 멀티스레드 환경에서도 데이터 무결성을 보장합니다.

---

### 4. 병목 전략 및 성능 개선 (Bottleneck Strategy & Performance)
서버 성능의 핵심인 네트워크와 DB 처리에서 발생하는 병목을 제거하기 위한 전략입니다.

*   **비동기 쿼리 시스템 (Command Pattern)**: 커맨드 패턴 기반의 `CQuery_Player` 시스템을 도입하여 메인 로직 스레드가 DB I/O를 기다리지 않도록 비동기 처리합니다. ADO Agent와 스레드 풀을 통해 쿼리를 분산 실행합니다.
*   **IOCP 기반 고성능 네트워킹**: Windows 환경에서 최적의 성능을 발휘하는 IOCP(Input/Output Completion Port)를 활용하여 수만 개의 소켓 연결과 대량의 패킷을 저지연으로 처리합니다.
*   **실시간 성능 모니터링**: `CThreadPerfManager`를 통해 각 스레드의 점유율, 처리 시간, 대기 시간을 실시간으로 수집하여 잠재적인 병목 지점을 사전에 파악합니다.
*   **메모리 최적화 및 풀링**: 빈번한 객체 생성/소멸로 인한 메모리 파편화를 방지하기 위해 풀링 기법을 적용하고, 스마트 포인터를 사용해 안정적인 메모리 관리를 수행합니다.

---

### 5. 성능 개선 결과 (Key Achievements)
*   **DB 부하 분산**: 커맨드 패턴 적용 후 메인 로직 스레드 점유율 감소 및 쿼리 응답 속도 안정화.
*   **트래픽 처리량 향상**: 섹터 분할 및 패킷 통계(`PacketStatistics`) 분석을 통한 최적화로 동시 접속자 수용 한계 대폭 상향.
*   **시스템 안정성**: 분산 서버 구조 도입을 통해 특정 서버 점검 시에도 다른 서버의 독립적인 운영 가능 여건 마련.

---
**Keywords**: `Distributed Game Server`, `IOCP`, `Multi-threading`, `Asynchronous DB Query`, `Channel/Sector Partitioning`, `Performance Tuning`.
