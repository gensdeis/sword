### MMORPG 서버 아키텍처 및 설계 분석

본 문서는 현재 프로젝트의 고가용성 MMORPG 서버 아키텍처를 이력서 및 기술 블로그용으로 정리한 기술 명세서입니다. 대규모 트래픽 처리와 안정적인 분산 시스템 설계에 초점을 맞추고 있습니다.

---

### 1. 아키텍처 구조 (Architecture Structure)
대규모 동시 접속자를 수용하기 위해 역할을 분리한 **분산 서버 아키텍처**를 채택하고 있습니다.

*   **Lobby Server**: 인증, 캐릭터 생성/선택 및 계정 레벨의 데이터 관리.
*   **Play Server**: 실제 게임 로직(전투, 이동, 퀘스트)이 이루어지는 메인 컨텐츠 서버.
*   **Match Server**: 파티 매칭 및 대전 컨텐츠를 전담하여 컨텐츠별 부하를 독립적으로 처리.
*   **Manage Server**: 서버 전체 상태 모니터링 및 실시간 설정 변경 관리.
*   **Common Library**: IOCP 기반 네트워크 라이브러리(SASIO), 공용 컨테이너, 데이터베이스 에이전트를 공유하여 일관성 있는 성능 유지.

---

### 2. 트래픽 급증 대응 전략 (Traffic Surge Strategy)
특정 지역이나 시간에 집중되는 트래픽을 효율적으로 분산하기 위해 **공간 및 로직 분할 기술**을 사용합니다.

*   **다중 채널 및 존(Zone) 시스템**: 동일 지역을 여러 채널로 분리하여 한 지역에 수천 명의 사용자가 몰리는 현상을 방지하고, 물리적 서버 자원을 유연하게 할당합니다.
*   **섹터(Sector) 기반 부하 분산**: 맵을 섹터 단위로 쪼개어 가시 범위(Interest Management) 내의 데이터만 전송함으로써 네트워크 대역폭 및 CPU 연산 비용을 최적화합니다.
*   **침공 및 필드 보스 컨텐츠 관리**: `InvasionManager`를 통해 동적인 컨텐츠 발생 시 해당 세션의 로직 부하를 관리하고 전용 에이전트(`InvasionAgent`)가 독립적인 생명주기를 가집니다.

---

### 3. 분산 처리 (Distributed Processing)
서버 간 부하 분산 및 효율적인 데이터 동기화를 위해 다음과 같은 기술을 적용했습니다.

*   **서버 역할 분할**: 인증(Lobby), 게임(Play), 매칭(Match) 서버를 물리적으로 분리하여 특정 서비스 장애가 전체 시스템으로 전파되는 것을 차단합니다.
*   **에이전트(Agent) 패턴**: 특정 기능(침공, 기여도, 연금술 등)을 담당하는 Agent 객체를 두어 비즈니스 로직을 모듈화하고 병렬 처리가 용이한 구조로 설계했습니다.
*   **TSafeContainer 기반 동기화**: `TSafeMap`, `TSafeList` 등 스레드 안전한 커스텀 컨테이너를 사용하여 멀티스레드 환경에서도 데이터 무결성을 보장합니다.

---

### 4. 병목 전략 및 성능 개선 (Bottleneck Strategy & Performance)
서버 성능의 핵심인 네트워크와 DB 처리에서 발생하는 병목을 제거하기 위한 전략입니다.

*   **비동기 쿼리 시스템 (Command Pattern)**: 커맨드 패턴 기반의 `CQuery_Player` 시스템을 도입하여 메인 로직 스레드가 DB I/O를 기다리지 않도록 비동기 처리합니다. ADO Agent와 스레드 풀을 통해 쿼리를 분산 실행합니다.
*   **IOCP 기반 고성능 네트워킹**: Windows 환경에서 최적의 성능을 발휘하는 IOCP(Input/Output Completion Port)를 활용하여 수만 개의 소켓 연결과 대량의 패킷을 저지연으로 처리합니다.
*   **실시간 성능 모니터링**: `CThreadPerfManager`를 통해 각 스레드의 점유율, 처리 시간, 대기 시간을 실시간으로 수집하여 잠재적인 병목 지점을 사전에 파악합니다.
*   **메모리 최적화 및 풀링**: 빈번한 객체 생성/소멸로 인한 메모리 파편화를 방지하기 위해 풀링 기법을 적용하고, 스마트 포인터를 사용해 안정적인 메모리 관리를 수행합니다.

---

### 5. 성능 개선 결과 (Key Achievements)
*   **DB 부하 분산**: 커맨드 패턴 적용 후 메인 로직 스레드 점유율 감소 및 쿼리 응답 속도 안정화.
*   **트래픽 처리량 향상**: 섹터 분할 및 패킷 통계(`PacketStatistics`) 분석을 통한 최적화로 동시 접속자 수용 한계 대폭 상향.
*   **시스템 안정성**: 분산 서버 구조 도입을 통해 특정 서버 점검 시에도 다른 서버의 독립적인 운영 가능 여건 마련.

---
**Keywords**: `Distributed Game Server`, `IOCP`, `Multi-threading`, `Asynchronous DB Query`, `Channel/Sector Partitioning`, `Performance Tuning`.

### MMORPG 서버 아키텍처 강점 및 약점 분석 보고서

본 문서는 현재 프로젝트의 아키텍처 구조를 객관적으로 분석하여, 시스템의 강점과 잠재적인 약점을 정리한 보고서입니다. 이 내용은 기술적인 의사결정 및 향후 개선 방향 수립에 활용될 수 있습니다.

---

### 1. 아키텍처 강점 (Strengths)

#### ① 높은 확장성 및 부하 분산 구조 (Scalability & Load Balancing)
*   **분산 서버 아키텍처**: Lobby, Play, Match, Manage 서버로 역할을 명확히 분리하여 기능별 독립적인 확장이 가능합니다. 특정 컨텐츠(예: 매칭)의 부하가 전체 게임 서비스에 미치는 영향을 최소화합니다.
*   **공간 분할 기술 (Channel/Sector)**: `Channel` 시스템을 통해 물리적 인원 제한을 극복하고, `Sector` 기반 가시 범위 관리(Interest Management)를 통해 불필요한 네트워크 패킷 전송을 획기적으로 줄여 대규모 트래픽 급증에 유연하게 대응합니다.

#### ② 고성능 네트워킹 및 데이터 처리 (High Performance)
*   **IOCP (Input/Output Completion Port) 활용**: Windows 환경에서 최적의 성능을 내는 IOCP 기반 네트워크 라이브러리(SASIO)를 사용하여 수만 명의 동시 접속자를 저지연으로 처리합니다.
*   **비동기 DB 커맨드 패턴**: 메인 로직 스레드와 DB I/O 스레드를 분리하고 커맨드 패턴으로 통신함으로써, DB 병목 현상이 게임 로직의 프레임 저하로 이어지지 않도록 설계되었습니다.

#### ③ 모듈화된 비즈니스 로직 (Modularity)
*   **에이전트(Agent) 패턴**: 침공(Invasion), 기여도(Contribution) 등 독립적인 컨텐츠 로직을 Agent 객체로 캡슐화하여 관리합니다. 이는 메인 클래스(CZone, CPlayer)의 비대화를 방지하고, 새로운 컨텐츠 추가 시 기존 코드에 미치는 영향을 최소화하는 SOLID 원칙을 준수합니다.

#### ④ 강력한 모니터링 및 진단 도구 (Observability)
*   **ThreadPerfManager & PacketStatistics**: 각 스레드의 CPU 점유율, 처리 시간 및 실시간 패킷 유입/유출량을 모니터링할 수 있는 체계가 구축되어 있습니다. 이는 라이브 서비스 중 발생하는 병목 지점을 즉각적으로 파악하고 최적화하는 데 매우 유리합니다.

---

### 2. 아키텍처 약점 (Weaknesses)

#### ① 분산 시스템의 복잡도 및 동기화 비용 (Complexity & Sync Overhead)
*   **데이터 일관성 유지**: 여러 서버(Play <-> Match) 간에 사용자 상태를 동기화하는 과정에서 레이스 컨디션이나 데이터 불일치가 발생할 위험이 있으며, 이를 해결하기 위한 `TSafeContainer` 등의 동기화 객체 사용으로 인한 오버헤드가 존재합니다.
*   **디버깅 난이도**: 분산 환경 특성상 여러 서버에 걸쳐 발생하는 트랜잭션을 추적하고 디버깅하는 것이 단일 서버 구조에 비해 복잡합니다.

#### ② 플랫폼 및 라이브러리 의존성 (Platform Dependency)
*   **Windows 전용 기술**: IOCP, ADO(COM 기반) 등 Windows OS에 최적화된 기술에 강하게 결합되어 있어, 최근 트렌드인 리눅스 기반 컨테이너 환경(Docker/K8s)으로의 전환이나 클라우드 네이티브 아키텍처로의 확장에 제약이 있습니다.

#### ③ 코드 유지보수 및 생산성 저하 (Maintenance & Productivity)
*   **DB 쿼리 보일러플레이트**: 신규 DB 기능 추가 시 다수의 Query 클래스를 생성하고 반복적인 파싱 코드를 작성해야 하는 번거로움이 있습니다. `Query_Player.cpp`와 같이 1만 라인이 넘어가는 대형 파일은 가독성을 저해하고 유지보수 비용을 높입니다.
*   **정적 데이터 관리**: 서버 간 공유되는 테이블 데이터나 설정값이 많아질수록 서버군 전체의 설정을 일관되게 관리하는 운영 부담이 증가합니다.

---

### 3. 개선 제안 및 기회 (Opportunities)

*   **DB 레이어 추상화**: 반복적인 SQL 파싱을 줄이기 위한 ORM 도입 또는 자동화된 코드 생성 도구를 활용하여 생산성을 높일 수 있습니다.
*   **크로스 플랫폼 검토**: 네트워크 엔진 및 핵심 라이브러리를 표준 C++ 또는 플랫폼 독립적인 라이브러리(예: asio)로 점진적으로 교체하여 향후 리눅스 환경 지원 가능성을 열어둘 수 있습니다.
*   **통합 운영 툴 고도화**: Manage 서버의 기능을 강화하여 서버군의 배포, 설정 변경, 로그 수집을 자동화함으로써 운영 리스크를 줄일 수 있습니다.

---
**Keywords**: `Architecture Analysis`, `SWOT`, `Technical Debt`, `Performance Tuning`, `Distributed Systems`.
